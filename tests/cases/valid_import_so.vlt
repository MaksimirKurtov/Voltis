import "kernel32.so";

extern fn GetCurrentProcessId() -> int32 from "kernel32.so";

public fn main() -> int32 {
    int32 pid = GetCurrentProcessId();
    print("pid: " + pid.ToString());
    return 0;
}

